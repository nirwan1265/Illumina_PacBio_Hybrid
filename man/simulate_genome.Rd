% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/script_wrappers.R
\name{simulate_genome}
\alias{simulate_genome}
\title{Simulate a genome with tandem/motif repeats (script wrapper)}
\usage{
simulate_genome(
  out_fa,
  in_fa = NULL,
  random_genome = FALSE,
  script_dir = file.path("scripts", "mac"),
  ...
)
}
\arguments{
\item{out_fa}{Character. Output FASTA path (required).}

\item{in_fa}{Character. Input FASTA path (optional unless `random_genome=TRUE`).}

\item{random_genome}{Logical. If TRUE, generate a random genome instead of using `in_fa`.}

\item{script_dir}{Character. Directory containing mac scripts (default `scripts/mac`).}

\item{...}{Additional named options passed as `--key value` to the script.}
}
\value{
TRUE on success.
}
\description{
Wrapper around `scripts/mac/01_make_tandem_repeats.R`. Use this to insert
tandem repeats, motif repeats, or both into an input FASTA, or to generate
a random genome with repeat insertions.
}
\examples{
\dontrun{
simulate_genome(out_fa = "01_simref/ecoli_repMed.fa", in_fa = "00_ref/ecoli.fa", n_events = 15, seg_len = 1000, copies = 5)
}
}
